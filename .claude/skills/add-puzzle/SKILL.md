---
name: add-puzzle
description: Alias of add-question, kept so old muscle memory (`/add-puzzle`) still works. Trigger this exactly as you would add-question — adding a question/puzzle to any domain of this placement-prep dashboard — then immediately follow .claude/skills/add-question/SKILL.md instead of anything here.
---

# Add Puzzle (alias)

This skill was renamed to **add-question** when the site grew from
quant-only puzzles to multiple domains. This file is kept only so
`/add-puzzle` still works out of habit — see `src/data/domains.js` for the
current domain list rather than restating it here, since it can change.

Do not follow any instructions from this file beyond this pointer. Instead,
read and follow **[.claude/skills/add-question/SKILL.md](../add-question/SKILL.md)**
in full, exactly as if the user had invoked `/add-question`.

If `.claude/` sits one level above the site root, the same relative path
still applies from wherever this file lives — `../add-question/SKILL.md`.
